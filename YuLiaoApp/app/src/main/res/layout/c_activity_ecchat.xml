<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/rl_ecchat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:padding="@dimen/ten_dp">

        <Button
            android:id="@+id/btn_ecchat_send_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:text="Send" />

        <EditText
            android:id="@+id/et_ecchat_msg"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toLeftOf="@id/btn_ecchat_send_msg"
            android:background="@drawable/c_shape_ecmain"
            android:drawableLeft="@mipmap/pen_black"
            android:padding="@dimen/ten_dp" />
    </RelativeLayout>

    <TextView
        android:id="@+id/tv_ecchat_show"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/rl_ecchat"
        android:maxLines="15"
        android:scrollbars="vertical" />

</RelativeLayout>